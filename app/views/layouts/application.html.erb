<!DOCTYPE html>
<html lang="en-GB" class="h-full bg-base-100 text-base-content" data-theme="light">
  <head>
    <%# Google Tag Manager - must be first in head for accurate tracking %>
    <%= render "layouts/google_tag_manager" %>

    <%# Block search engine indexing on staging domains %>
    <% if request.host.include?("staging.afida.com") %>
      <meta name="robots" content="noindex, nofollow">
    <% end %>

    <title><%= content_for(:title) %></title>
    <meta name="description" content="<%= content_for(:meta_description) %>">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>
    <%= canonical_url %>

    <%= vite_client_tag if Rails.env.development? %>
    <%= vite_javascript_tag "application" %>
    <%= vite_stylesheet_tag "application" %>

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <%= Sentry.get_trace_propagation_meta.html_safe %>
  </head>

  <body class="flex flex-col h-screen">
    <%= render "layouts/google_tag_manager_noscript" %>

    <%= tag.div notice, class: "alert alert-info", data: { turbo_temporary: true } if notice %>
    <%= tag.div alert, class: "alert alert-error", data: { turbo_temporary: true } if alert %>

    <%# Global toast container for Turbo Stream notifications %>
    <div id="toast-container" class="fixed top-20 right-4 z-50 space-y-2"></div>

    <%= render "shared/navbar" %>
    <%= render "shared/category_nav" %>

    <main class="container mx-auto px-5 pb-16 flex-grow">
      <%= yield :breadcrumbs %>
      <%= yield %>
    </main>

    <%= render "shared/footer" %>

    <%# Quick Add Modal - Single shared instance %>
    <%= render "shared/quick_add_modal" %>

    <%# Search Modal %>
    <%= render "shared/search_modal" %>

    <%# Beautiful Turbo Confirm Dialog %>
    <%= render "shared/confirm_dialog" %>
  </body>
</html>
