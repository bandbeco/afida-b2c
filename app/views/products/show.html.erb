<% if @product.customizable_template? %>
  <%= render "branded_configurator", product: @product %>
<% else %>
  <%# Unified variant selector for all standard products (multi-option, single-option, quantity-only) %>
  <%= render "variant_selector", product: @product, selected_variant: @selected_variant %>
<% end %>
